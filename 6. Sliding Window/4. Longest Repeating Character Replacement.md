# Solution
```python
class Solution:
    def characterReplacement(self, s: str, k: int) -> int:

        l = 0
        n = len(s)
        longest = 0
        counts = [0] * 26

        for r in range(n):
            counts[ord]



```